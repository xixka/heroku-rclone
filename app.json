{
    "name": "rclone",
    "description": "Deploy rclone on Heroku.",
    "keywords": ["rclone"],
    "env": {
        "rclone": {
            "description": "$HOME/.rclone.conf",
            "value": "",
            "required": false
        },
        "rcloneforurl": {
            "description": "通过url加载rclone配置（为空不加载）",
            "value": "",
            "required": false
        }
    },
    "stack": "container"
}
